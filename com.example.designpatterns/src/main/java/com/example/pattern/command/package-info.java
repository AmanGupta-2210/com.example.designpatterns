/**
 * @author Aman Gupta (aman.tit10@gmail.com)
 * @version 1.0
 * Design Pattern : Command
 * Description : In Command Pattern, A request is wrapped under a object as command and passed to invoker object. 
 * Invoker object looks for the appropriate object which can handle this command and pass the command to the corresponding object and 
 * that object executes the command.
 */
package com.example.pattern.command;